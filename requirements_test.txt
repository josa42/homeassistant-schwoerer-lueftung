pytest-homeassistant-custom-component==0.13.364

# Provided at runtime by the `modbus` integration (see its manifest.json), so
# they are not listed in our own manifest requirements. Tests import them
# directly, so they have to be installed here. Keep the pins in step with the
# modbus integration for the Home Assistant version above.
modbus-connection[tmodbus]==4.10.0
tmodbus==0.6.2
pymodbus==3.13.1
